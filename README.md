#  DATA MINING ASSIGNMENTS
## Subject code: CSE1816PE21
- *Total Assignment = 4*
<br>
- **1. Digit recognization**
-- A machine learning project that uses a neural network (trained on the MNIST dataset) to recognize handwritten digits from images. The model predicts digits (0–9) based on pixel patterns.
<br>
- 2. **KNN-IRIS**
-- A classification project using the K-Nearest Neighbors algorithm on the Iris dataset to classify flowers into species based on features like petal and sepal measurements.
<br>
- 3. **Spam filtering**
-- A text classification project that identifies whether a message is spam or not using machine learning techniques such as Naive Bayes and text preprocessing methods like tokenization and vectorization.
<br>
- 4. **cancer prediction**
-- A predictive model that analyzes medical data to determine the likelihood of cancer. It uses classification algorithms to distinguish between benign and malignant cases based on input features.